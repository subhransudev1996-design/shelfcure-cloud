// @shelfcure/core — pure-TS business logic. Zero React, zero Tauri, zero browser-only deps.
// Phase 1 will populate: gst, billMath, stockMath, draft, validators, formatters, gstr1Export, barcodeUtils, csvPurchaseParser.
// Mirrored in Dart (mobile/lib/core/) with cross-language parity tests in CI.
export {};
