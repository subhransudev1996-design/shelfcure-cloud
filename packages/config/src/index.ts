// @shelfcure/config — shared ESLint and TS preset configs.
// Phase 0: stub. Phase 1 will populate eslint-base / eslint-react / tsconfig presets and migrate root configs to consume from here.
export {};
