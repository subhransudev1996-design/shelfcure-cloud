// @shelfcure/sync-engine — offline outbox, conflict resolution, cursor tracking.
// Phase 2 will populate: outbox queue, push worker, pull worker, conflict resolver, realtime subscriber, deterministic test harness.
// Riskiest code in the project. Every change needs scenario tests (see plan §15.1).
export {};
